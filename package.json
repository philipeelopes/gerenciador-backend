{
  "name": "gerenciador-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx json-server --watch db.json --host 0.0.0.0 --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
