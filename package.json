{
  "name": "gerenciador-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node ./node_modules/json-server/dist/bin.js --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": "18.x"
  }
}

